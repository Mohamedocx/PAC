# PAC (Personal Address Code) System

## نظرة عامة

PAC (Personal Address Code) هو نظام عنونة شخصي يتيح تحويل الموقع الجغرافي إلى عنوان نصي قصير قابل للمشاركة والحفظ، مثل رقم الهاتف.

## لماذا PAC؟

### المشكلة
- العناوين التقليدية معقدة وطويلة
- صعوبة مشاركة المواقع الدقيقة
- الإحداثيات الجغرافية (lat/lng) غير عملية للاستخدام اليومي

### الحل
PAC يحول الموقع الجغرافي إلى رمز قصير مثل:
- `THTQ-9C8K-7` (منزل)
- `THTQ-9C8K-7 / F3-A02` (شقة في الطابق الثالث)

## المميزات الرئيسية

### 🔒 الأمان والخصوصية
- ✅ **لا قاعدة بيانات للعناوين**: النظام لا يخزن أي مواقع أو عناوين
- ✅ **لا تسجيل مستخدمين**: لا حاجة لإنشاء حسابات
- ✅ **عمل محلي**: الخوارزمية تعمل بالكامل على جهاز المستخدم
- ✅ **لا تسجيل logs**: الـ API لا يسجل أي payloads

### 🎯 الدقة
- **Precision 8**: دقة ~19 متر (مناسب للمنازل)
- **Precision 9**: دقة ~2.4 متر (دقة عالية)

### 🛡️ الحماية
- Check Digit للكشف عن الأخطاء
- API Keys للمصادقة
- Rate Limiting لمنع الإساءة
- Anti-bot protection

### 🌍 قابلية الدمج
- مكتبة C# (.NET)
- SDK TypeScript/JavaScript
- REST API
- توثيق OpenAPI/Swagger

## كيف يعمل؟

### 1. التشفير (Encoding)
```
الموقع الجغرافي → Geohash → Check Digit → PAC Code
(31.2357, 30.0444) → stq4s3x1 → 7 → STQ4-S3X1-7
```

### 2. فك التشفير (Decoding)
```
PAC Code → Validate Check Digit → Geohash → الموقع الجغرافي
STQ4-S3X1-7 → ✓ → stq4s3x1 → (31.2357, 30.0444)
```

### 3. التحقق (Validation)
```
PAC Code → Check Format → Validate Check Digit → ✓/✗
```

## البنية المعمارية

```
┌─────────────────────────────────────────────────┐
│                  Applications                    │
│  (Mobile Apps, Web Apps, Desktop Apps)          │
└────────────┬────────────────────────┬────────────┘
             │                        │
             ▼                        ▼
    ┌────────────────┐      ┌─────────────────┐
    │   PAC SDK      │      │   PAC API       │
    │  (Local Only)  │      │  (Stateless)    │
    └────────┬───────┘      └────────┬────────┘
             │                       │
             ▼                       ▼
    ┌─────────────────────────────────────┐
    │         PAC Core Algorithm          │
    │  (Geohash + Check Digit)            │
    └─────────────────────────────────────┘
```

## حالات الاستخدام

### 1. التوصيل والشحن
```
العميل: "عنواني هو THTQ-9C8K-7 / F3-A02"
السائق: يدخل الكود → يحصل على الموقع الدقيق
```

### 2. الطوارئ
```
المتصل: "أنا في موقع R3GX-2F77-M"
الإسعاف: تحديد الموقع فوراً
```

### 3. العقارات
```
الإعلان: "شقة للبيع - PAC: STQ4-S3X1-7 / F5-A12"
المشتري: يرى الموقع الدقيق على الخريطة
```

### 4. مشاركة المواقع
```
صديق: "تعال إلى UPBP-BPBP-Q"
أنت: تفتح الموقع في خرائط Google
```

## الفرق بين PAC والأنظمة الأخرى

| الميزة | PAC | Plus Codes | What3Words |
|--------|-----|------------|------------|
| الطول | 11-13 حرف | 10-11 حرف | 3 كلمات |
| Check Digit | ✅ | ❌ | ❌ |
| Open Source | ✅ | ✅ | ❌ |
| لا قاعدة بيانات | ✅ | ✅ | ❌ (يحتاج DB) |
| دعم الشقق | ✅ | ❌ | ❌ |
| عمل محلي | ✅ | ✅ | ❌ |

## البدء السريع

### استخدام SDK محلياً (بدون إنترنت)

#### C#
```csharp
using PAC.Core;

// Encode
string pac = PACCore.Encode(31.2357, 30.0444, 8);
// Result: "STQ4-S3X1-7"

// Decode
var result = PACCore.Decode("STQ4-S3X1-7");
// Result: { IsValid: true, Latitude: 31.2357, Longitude: 30.0444 }
```

#### TypeScript/JavaScript
```typescript
import { encode, decode } from '@pac/core';

// Encode
const pac = encode({ latitude: 31.2357, longitude: 30.0444 });
// Result: "STQ4-S3X1-7"

// Decode
const result = decode("STQ4-S3X1-7");
// Result: { isValid: true, latitude: 31.2357, longitude: 30.0444 }
```

### استخدام API

```bash
# Encode
curl -X POST https://api.pac.example.com/v1/pac/encode \
  -H "X-API-Key: your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"latitude": 31.2357, "longitude": 30.0444, "precision": 8}'

# Decode
curl -X POST https://api.pac.example.com/v1/pac/decode \
  -H "X-API-Key: your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"pacCode": "STQ4-S3X1-7"}'
```

## الأمان

### ما يتم تخزينه
- ✅ مفاتيح API للعملاء فقط (في جدول صغير)
- ❌ **لا يتم تخزين**: عناوين، مواقع، إحداثيات، أو أي بيانات جغرافية

### ما يتم تسجيله في Logs
- ✅ طلبات API (timestamps, IP, endpoint)
- ✅ أخطاء النظام
- ❌ **لا يتم تسجيل**: PAC codes، إحداثيات، أو أي payloads

### الحماية
1. **API Keys**: كل طلب يحتاج مفتاح صالح
2. **Rate Limiting**: 100 طلب/دقيقة للمصادقين، 10 للضيوف
3. **Anti-bot**: تحدي تكيفي عند الاشتباه
4. **HTTPS Only**: كل الاتصالات مشفرة
5. **CORS**: قائمة بيضاء للنطاقات المسموحة

## الترخيص

MIT License - مفتوح المصدر ومجاني للاستخدام التجاري

## الدعم

- 📖 [التوثيق الكامل](./API.md)
- 🔧 [أمثلة التكامل](../examples/)
- 🧪 [Test Vectors](./test-vectors.md)
- 🐛 [الإبلاغ عن مشكلة](https://github.com/pac/issues)

---

**PAC** - عنوانك الشخصي، بسيط وآمن وخاص 🔒
