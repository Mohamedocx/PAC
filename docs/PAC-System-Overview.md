# PAC System - نظام الرمز الشخصي للعنوان
## Personal Address Code System

---

## 📋 نظرة عامة / Overview

### ما هو نظام PAC؟
نظام **PAC (Personal Address Code)** هو نظام مبتكر لتحويل المواقع الجغرافية (خطوط الطول والعرض) إلى رموز نصية قصيرة وسهلة المشاركة، مشابهة لأرقام الهواتف. يهدف النظام إلى تبسيط مشاركة العناوين والمواقع بطريقة آمنة وخصوصية.

**PAC (Personal Address Code)** is an innovative system that converts geographic locations (latitude/longitude) into short, shareable text codes, similar to phone numbers. The system aims to simplify address and location sharing in a secure and privacy-focused manner.

---

## 🎯 الهدف الرئيسي / Main Objective

تحويل الإحداثيات الجغرافية المعقدة مثل:
```
Latitude: 30.0444196, Longitude: 31.2357116
```

إلى رمز بسيط وقصير مثل:
```
stq4s3x1-8-3-A5
```

حيث يمكن مشاركة هذا الرمز بسهولة عبر الرسائل، البريد الإلكتروني، أو أي وسيلة تواصل أخرى.

---

## 🏗️ البنية المعمارية / System Architecture

النظام مبني على **4 مكونات رئيسية**:

### 1. **PAC Core Library** (المكتبة الأساسية)
- **اللغات**: C# و TypeScript
- **الوظيفة**: تحتوي على الخوارزميات الأساسية لتشفير وفك تشفير رموز PAC
- **المميزات**:
  - خوارزمية Geohash لتحويل الإحداثيات
  - Check Digit للتحقق من صحة الرمز
  - دعم الطوابق والشقق (Floor/Apartment)
  - Cross-language compatibility (توافق بين اللغات)

### 2. **PAC API** (واجهة برمجة التطبيقات)
- **التقنية**: .NET 8/9 Minimal API
- **النوع**: Stateless API (بدون تخزين بيانات)
- **الأمان**:
  - API Key Authentication (مصادقة بمفتاح API)
  - Rate Limiting (تحديد معدل الطلبات)
  - CORS Support (دعم الطلبات من نطاقات مختلفة)
  - Anti-bot Protection (حماية ضد البوتات)

### 3. **SDKs** (مجموعات تطوير البرمجيات)
- **SDK للـ .NET**: لتطبيقات C#
- **SDK للـ TypeScript**: لتطبيقات JavaScript/TypeScript
- **الهدف**: تسهيل استخدام النظام للمطورين

### 4. **PAC Web** (واجهة المستخدم)
- **التقنية**: React + Vite + Tailwind CSS v4
- **المميزات**:
  - واجهة مستخدم حديثة وجذابة
  - دعم اللغة العربية والإنجليزية
  - تشفير محلي في المتصفح (Client-side)
  - لا يتطلب حساب مستخدم

---

## 🔐 الأمان والخصوصية / Security & Privacy

### المبادئ الأساسية:
1. **عدم تخزين البيانات**: النظام لا يحفظ أي عناوين أو مواقع
2. **عدم تسجيل المستخدمين**: لا يتطلب إنشاء حساب
3. **عدم تسجيل البيانات الحساسة**: لا يتم تسجيل الإحداثيات أو الرموز
4. **API Key Protection**: حماية الـ API بمفاتيح مصادقة
5. **Rate Limiting**: 
   - 100 طلب/دقيقة للمصادقين
   - 10 طلب/دقيقة بدون مصادقة

---

## 📝 صيغة رمز PAC / PAC Code Format

### الصيغة الأساسية:
```
GEOHASH-CHECKDIGIT
```
**مثال**: `stq4s3x1-8`

### الصيغة الكاملة (مع الطابق والشقة):
```
GEOHASH-CHECKDIGIT-FLOOR-APARTMENT
```
**مثال**: `stq4s3x1-8-3-A5`

### مكونات الرمز:
1. **Geohash** (8 أو 9 أحرف): يمثل الموقع الجغرافي
2. **Check Digit** (رقم واحد): للتحقق من صحة الرمز
3. **Floor** (اختياري): رقم الطابق
4. **Apartment** (اختياري): رقم/حرف الشقة

---

## ⚙️ العمليات الأساسية / Core Operations

### 1. **Encode** (التشفير)
تحويل الإحداثيات الجغرافية إلى رمز PAC

**Input:**
```json
{
  "latitude": 30.0444,
  "longitude": 31.2357,
  "precision": 8,
  "floor": 3,
  "apartment": "A5"
}
```

**Output:**
```json
{
  "pacCode": "stq4s3x1-8-3-A5",
  "precision": 8
}
```

### 2. **Decode** (فك التشفير)
تحويل رمز PAC إلى إحداثيات جغرافية

**Input:**
```json
{
  "pacCode": "stq4s3x1-8-3-A5"
}
```

**Output:**
```json
{
  "latitude": 30.0444,
  "longitude": 31.2357,
  "precision": 8,
  "floor": 3,
  "apartment": "A5"
}
```

### 3. **Validate** (التحقق)
التحقق من صحة رمز PAC

**Input:**
```json
{
  "pacCode": "stq4s3x1-8"
}
```

**Output (Valid):**
```json
{
  "isValid": true,
  "precision": 8,
  "reason": null
}
```

**Output (Invalid):**
```json
{
  "isValid": false,
  "precision": null,
  "reason": "Invalid check digit"
}
```

### 4. **Normalize** (التطبيع)
توحيد صيغة رمز PAC

**Input:**
```json
{
  "pacCode": "  STQ4S3X1-8  "
}
```

**Output:**
```json
{
  "normalizedPacCode": "stq4s3x1-8"
}
```

---

## 🎚️ مستويات الدقة / Precision Levels

| Precision | Cell Size | Use Case |
|-----------|-----------|----------|
| 1 | ±2500 km | Country |
| 2 | ±630 km | Large region |
| 3 | ±78 km | City |
| 4 | ±20 km | District |
| 5 | ±2.4 km | Neighborhood |
| 6 | ±610 m | Street |
| 7 | ±76 m | Building block |
| **8** | **±19 m** | **Building (Default)** |
| 9 | ±2.4 m | Apartment/Room |
| 10 | ±60 cm | Very precise |
| 11 | ±7.4 cm | Extremely precise |
| 12 | ±1.9 cm | Maximum precision |

**الدقة الافتراضية**: 8 (مناسبة للمباني)

---

## 🔧 التقنيات المستخدمة / Technologies Used

### Backend:
- **.NET 8/9**: إطار العمل الرئيسي
- **Minimal API**: لبناء API خفيف وسريع
- **Geohash Algorithm**: لتشفير الإحداثيات
- **HMAC-SHA256**: للمصادقة

### Frontend:
- **React 18**: مكتبة بناء واجهة المستخدم
- **Vite**: أداة البناء السريعة
- **Tailwind CSS v4**: إطار التصميم
- **TypeScript**: للكتابة الآمنة

### Testing:
- **xUnit**: لاختبار الكود
- **Test Vectors**: لضمان التوافق بين اللغات

---

## 📊 API Endpoints

### Health Endpoints (لا تتطلب مصادقة):
- `GET /healthz` - فحص صحة النظام
- `GET /readyz` - فحص جاهزية النظام

### PAC Endpoints (تتطلب API Key):
- `POST /v1/pac/encode` - تشفير الإحداثيات
- `POST /v1/pac/decode` - فك تشفير الرمز
- `POST /v1/pac/validate` - التحقق من الرمز
- `POST /v1/pac/normalize` - تطبيع الرمز

---

## 💡 حالات الاستخدام / Use Cases

### 1. **خدمات التوصيل / Delivery Services**
```csharp
// مثال: شركة توصيل تستخدم PAC لتحديد العناوين
var deliveryAddress = "stq4s3x1-8-3-A5";
var location = PACCore.Decode(deliveryAddress);
// يمكن الآن توجيه السائق للموقع الدقيق
```

### 2. **مشاركة المواقع / Location Sharing**
بدلاً من إرسال رابط خرائط طويل، يمكن إرسال:
```
"تعال على العنوان: stq4s3x1-8"
```

### 3. **أنظمة الطوارئ / Emergency Services**
```
"الحالة الطارئة في: u4pruydq-9-2-B12"
```

### 4. **حجوزات الفنادق / Hotel Reservations**
```json
{
  "hotelName": "Grand Hotel",
  "pacCode": "stq4s3x1-8",
  "room": "305"
}
```

### 5. **تطبيقات العقارات / Real Estate**
```
Property ID: stq4s3x1-8-5-C7
```

---

## 🌐 الواجهة الأمامية / Frontend Features

### المكونات الرئيسية:

#### 1. **EncodeView** (واجهة التشفير)
- إدخال خطوط الطول والعرض
- اختيار مستوى الدقة
- إضافة الطابق والشقة (اختياري)
- عرض الرمز الناتج
- نسخ الرمز بضغطة واحدة

#### 2. **DecodeView** (واجهة فك التشفير)
- إدخال رمز PAC
- عرض الإحداثيات الناتجة
- عرض معلومات الطابق والشقة
- عرض الموقع على الخريطة (اختياري)

#### 3. **ValidateView** (واجهة التحقق)
- إدخال رمز PAC
- التحقق من صحته
- عرض سبب الخطأ إن وجد

---

## 🧪 الاختبارات / Testing

### Test Vectors (متجهات الاختبار):
النظام يستخدم test vectors لضمان التوافق بين C# و TypeScript:

```json
{
  "latitude": 30.0444196,
  "longitude": 31.2357116,
  "precision": 8,
  "expectedGeohash": "stq4s3x1",
  "expectedCheckDigit": 8,
  "expectedPAC": "stq4s3x1-8"
}
```

### أنواع الاختبارات:
1. **Unit Tests**: اختبار كل دالة على حدة
2. **Integration Tests**: اختبار تكامل المكونات
3. **Cross-language Tests**: التأكد من توافق C# و TypeScript
4. **API Tests**: اختبار نقاط الـ API

---

## 📈 الأداء / Performance

### مميزات الأداء:
- **Stateless**: لا يحتاج لقاعدة بيانات
- **Fast**: التشفير وفك التشفير يتم في ميلي ثانية
- **Scalable**: يمكن توسيع النظام أفقياً
- **Lightweight**: حجم صغير وسريع

### Rate Limiting:
- **Authenticated**: 100 requests/minute
- **Unauthenticated**: 10 requests/minute

---

## 🔄 سير العمل / Workflow

### 1. تشفير موقع جديد:
```
User Input (Lat/Lng) 
  → Frontend Validation 
  → API Call (/v1/pac/encode) 
  → PAC Core Processing 
  → Generate Geohash 
  → Calculate Check Digit 
  → Return PAC Code
```

### 2. فك تشفير رمز PAC:
```
User Input (PAC Code) 
  → Frontend Validation 
  → API Call (/v1/pac/decode) 
  → PAC Core Processing 
  → Validate Check Digit 
  → Decode Geohash 
  → Return Coordinates
```

---

## 📚 الوثائق / Documentation

### Swagger UI:
النظام يوفر واجهة Swagger تفاعلية على:
```
http://localhost:5000/swagger
```

### المميزات:
- توثيق ثنائي اللغة (عربي/إنجليزي)
- أمثلة عملية لكل endpoint
- إمكانية تجربة الـ API مباشرة
- شرح تفصيلي للمعاملات والاستجابات

---

## 🚀 كيفية البدء / Getting Started

### 1. تشغيل الـ API:
```bash
cd src/PAC.API
dotnet run
```
الـ API سيعمل على: `http://localhost:5000`

### 2. تشغيل الواجهة الأمامية:
```bash
cd src/PAC.Web
npm install
npm run dev
```
الواجهة ستعمل على: `http://localhost:5173`

### 3. الوصول للـ Swagger:
افتح المتصفح على: `http://localhost:5000/swagger`

---

## 🎨 التصميم / Design Principles

### 1. **Privacy First** (الخصوصية أولاً)
- لا تخزين للبيانات
- لا تسجيل للمستخدمين
- معالجة محلية في المتصفح

### 2. **Simple & Intuitive** (بسيط وسهل)
- واجهة نظيفة
- خطوات واضحة
- رسائل خطأ مفهومة

### 3. **Secure by Default** (آمن افتراضياً)
- API Key مطلوب
- Rate Limiting مفعل
- CORS محدد

### 4. **Bilingual Support** (دعم ثنائي اللغة)
- عربي وإنجليزي
- واجهة قابلة للتبديل
- توثيق بكلا اللغتين

---

## 🔮 التطوير المستقبلي / Future Enhancements

### المخطط:
1. **Mobile Apps**: تطبيقات iOS و Android
2. **QR Code Integration**: دمج رموز QR
3. **Map Integration**: عرض على الخرائط
4. **Offline Mode**: وضع عدم الاتصال
5. **Bulk Operations**: عمليات جماعية
6. **Analytics Dashboard**: لوحة تحليلات
7. **Multi-language Support**: دعم لغات إضافية

---

## 📞 الدعم / Support

### الموارد:
- **Swagger Documentation**: `http://localhost:5000/swagger`
- **GitHub Repository**: (رابط المشروع)
- **API Health Check**: `http://localhost:5000/healthz`

---

## 📄 الترخيص / License

 - مفتوح المصدر

---

## 👥 الفريق / Team

PAC Developmed by Mohamed Ahmed 
- 
[GitHub](https://github.com/Mohamedocx) • [LinkedIn](https://www.linkedin.com/in/mohamed-ahmed-yousif/)


---

## 🏁 الخلاصة / Summary

نظام **PAC** هو حل مبتكر وآمن لتحويل المواقع الجغرافية إلى رموز قصيرة وسهلة المشاركة. يجمع النظام بين البساطة في الاستخدام والأمان في التصميم، مع التركيز على الخصوصية وعدم تخزين البيانات. مثالي لخدمات التوصيل، مشاركة المواقع، وأي تطبيق يحتاج لنظام عناوين بسيط وفعال.

**PAC System** is an innovative and secure solution for converting geographic locations into short, shareable codes. The system combines ease of use with security in design, focusing on privacy and no data storage. Perfect for delivery services, location sharing, and any application that needs a simple and effective addressing system.
